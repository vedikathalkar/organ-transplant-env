[project]
name = "organ-transplant-env"
version = "1.0.0"
description = "Organ Transplant Matching and Logistics OpenEnv"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "httpx",
    "openai",
]
