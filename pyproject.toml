[project]
name = "organ-transplant-env"
version = "1.0.0"
description = "Organ Transplant Matching OpenEnv"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "httpx",
    "openai",
    "openenv-core>=0.2.0",
]

[project.scripts]
server = "app.main:app"